list(APPEND CPPFLAGS -DTORONTO)
list(APPEND LDFLAGS -lm)
if(LARGE_PROBLEM_SIZE)
  set(RUN_OPTIONS 40000000)
  set(HASH_PROGRAM_OUTPUT 1)
else()
  set(RUN_OPTIONS 700000)
endif()
llvm_multisource(bisort)

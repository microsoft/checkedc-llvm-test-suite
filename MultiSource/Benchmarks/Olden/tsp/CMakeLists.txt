list(APPEND CPPFLAGS -DTORONTO)
list(APPEND LDFLAGS -lm)
if(LARGE_PROBLEM_SIZE)
  set(RUN_OPTIONS 40960000)
else()
  if(SMALL_PROBLEM_SIZE)
    set(RUN_OPTIONS 102400)
  else()
    set(RUN_OPTIONS 1024000)
  endif()
endif()
llvm_multisource(tsp)

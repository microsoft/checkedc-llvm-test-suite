set(PROG ks)
if(LARGE_PROBLEM_SIZE)
  list(APPEND RUN_OPTIONS ${CMAKE_CURRENT_SOURCE_DIR}/KL-5.in)
  set(HASH_PROGRAM_OUTPUT 1)
else()
  list(APPEND RUN_OPTIONS ${CMAKE_CURRENT_SOURCE_DIR}/KL-4.in)
endif()
llvm_multisource()
